import { FixedValuePipe } from './fixed-value.pipe';

describe('FixedValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FixedValuePipe();
    expect(pipe).toBeTruthy();
  });
});
